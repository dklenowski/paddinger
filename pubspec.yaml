name: paddinger_packages
description: Base pubspec.yaml for melos.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 2.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  melos: ^3.4.0
  paddinger_annotations:
    path: packages/paddinger_annotations

dev_dependencies:
  build_runner: ^2.4.1
  paddinger:
    path: packages/paddinger

#dependency_overrides:
#  meta: 1.7.0

#flutter:
#  uses-material-design: true
